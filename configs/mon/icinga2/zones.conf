// /etc/icinga2/zones.conf — mon VM
// Single-zone master setup. No satellites or agents — all checks run from mon
// over the AS215932 IPv6 overlay network.

object Endpoint "mon.as215932.net" {
  host = "::1"  // localhost
}

object Zone "master" {
  endpoints = [ "mon.as215932.net" ]
}
