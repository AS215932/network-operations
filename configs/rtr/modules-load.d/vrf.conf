# /etc/modules-load.d/vrf.conf — rtr.ovh1
# Load vrf early so {tcp,udp}_l3mdev_accept sysctls exist when 99-rtr.conf
# applies. Without this, networkd loads vrf later → sysctls default to 0 →
# wildcard-bound services (sshd, node_exporter) reject overlay-VRF traffic.
vrf
