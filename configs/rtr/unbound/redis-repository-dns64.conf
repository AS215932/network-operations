server:
    # Native CloudFront IPv6 to the Redis APT repository is unreachable from
    # infra; both tested IPv4 endpoints work through the existing NAT64 path.
    # Keep normal DNS resolution and TLS hostname/signature validation.
    dns64-ignore-aaaa: packages.redis.io
