---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart hyrule-web
  ansible.builtin.systemd:
    name: "{{ hyrule_web_service_name }}"
    state: restarted
    enabled: true
