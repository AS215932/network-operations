# Review artifact only — never accepted as a runtime secrets file.
STALWART_PUBLIC_URL=https://{{ agent_mail_hostname }}
STALWART_DNS_TSIG_SECRET=<from-vault>
STALWART_WEBHOOK_SECRET=<from-vault>
{% if agent_mail_bootstrap_enabled | bool %}
STALWART_RECOVERY_ADMIN=admin:<temporary-from-vault>
{% endif %}
