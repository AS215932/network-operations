---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart agent-core-collector
  ansible.builtin.systemd:
    name: "{{ agent_core_collector_service_name }}"
    state: restarted
    daemon_reload: true
  when: agent_core_collector_enabled | bool
