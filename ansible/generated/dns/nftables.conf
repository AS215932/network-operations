#!/usr/sbin/nft -f
# /etc/nftables.conf — managed by ansible (roles/firewall)
# Host: dns
# Generated: do not edit by hand.

flush ruleset

table inet filter {
    chain input {
        type filter hook input priority filter; policy accept;

        # Loopback always allowed.
        iif lo accept

        # Established/related — keep returning packets flowing.
        ct state established,related accept
        ct state invalid drop

        # ICMPv6 — required for ND, RA, MTU discovery, ping.
        meta l4proto ipv6-icmp accept

        # ICMPv4 — keep ping working.
        meta l4proto icmp accept

        # SSH from VPN clients and the KPN ops prefix.
        ip6 saddr 2a02:a442:1016::/48 tcp dport 22 counter accept comment "ssh from 2a02:a442:1016::/48"
        ip6 saddr 2a0c:b641:b50:3::/64 tcp dport 22 counter accept comment "ssh from 2a0c:b641:b50:3::/64"
        ip saddr 77.166.211.126/32 tcp dport 22 counter accept comment "ssh from 77.166.211.126/32"

        # Per-host service allowances.
         tcp dport 53 counter accept comment "auth DNS from world"
         udp dport 53 counter accept comment "auth DNS from world"
         tcp dport 53 counter accept comment "auth DNS from world"
         udp dport 53 counter accept comment "auth DNS from world"
        ip6 saddr { 2a00:f10:121:400:4be:60ff:fe00:526, 2a05:d014:f80:6e00:c937:174c:45eb:a5f7 } tcp dport 53 counter accept comment "AXFR from Openprovider (v6)"
        ip saddr { 35.157.8.190, 18.203.73.190, 185.27.175.218 } tcp dport 53 counter accept comment "AXFR from Openprovider (v4)"
        ip6 saddr 2a02:a442:1016::/48 tcp dport 53 counter accept comment "AXFR from ops-prefix (v6)"
        ip saddr 77.166.211.126/32 tcp dport 53 counter accept comment "AXFR from ops-prefix (v4)"
        ip6 saddr { 2a0c:b641:b50:2::20, 2a0c:b641:b50:2::40 } tcp dport 53 counter accept comment "RFC 2136 dyn updates from api/proxy"
        ip6 saddr 2a0c:b641:b50:2::50 tcp dport 9100 counter accept comment "node_exporter scrape"


        # End of input — log+counter what would have been dropped if policy were drop.
        limit rate 10/second counter log prefix "fw-would-drop " accept
    }

    chain forward {
        type filter hook forward priority filter; policy drop;
        ct state established,related accept
        ct state invalid drop
    }

    chain output {
        type filter hook output priority filter; policy accept;
    }
}
